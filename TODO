- Make write() failures and server resetting the connection both be
  captured as the same error.

- More fingerprints
- starttls support
- http proxy support (CONNECT method)
- command line option for proxies
- make -l not print the matches
